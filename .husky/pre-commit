set -euo pipefail

ROOT="$(git rev-parse --show-toplevel)"
BASE="$ROOT/docker-compose.yml"
CI="$ROOT/.husky/docker-compose.ci.yml"
COMPOSE="docker compose -f $BASE -f $CI"

echo "🧹  Frontend lint-staged…"
pushd "$ROOT/frontend" >/dev/null
npx lint-staged
popd >/dev/null

echo "🧹  Backend pre-commit hooks…"
pushd "$ROOT/backend" >/dev/null
uv run pre-commit run --config .pre-commit-config.yaml --hook-stage pre-commit
popd >/dev/null

echo "✅  Linting and formatting checks passed"

git diff --cached --name-only | grep -qE "(models\.py|models/|alembic/versions/)" || {
  echo "ℹ️  No DB-relevant changes — skipping migration checks"; exit 0; }

echo "🗃️  Migration & model checks…"

$COMPOSE up -d --remove-orphans db

for i in {1..10}; do
  state=$($COMPOSE ps --format '{{.Health}}' db)
  [[ "$state" == "healthy" ]] && { echo "✅  DB healthy"; break; }
  [[ $i -eq 10 ]] && { echo "❌  DB not healthy in time"; exit 1; }
  echo "⏳  Waiting for DB ($i/10)…"; sleep 1
done

echo "🚀  Running test-migrations…"
CID=$($COMPOSE run --build --no-deps -d test-migrations | tail -n1)

EXIT=$(docker wait "$CID")
docker logs "$CID"

echo "🧹  Cleaning up test-migrations container…"
$COMPOSE stop   test-migrations >/dev/null 2>&1 || true
$COMPOSE rm -f  test-migrations >/dev/null 2>&1 || true

if [[ "$EXIT" -eq 0 ]]; then
  echo "✅  Migration & model checks passed"
else
  echo "❌  Migration checks failed (exit $EXIT)"
  exit 1
fi
